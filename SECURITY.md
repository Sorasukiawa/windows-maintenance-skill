# 安全与隐私

脚本用于本地只读采集及新报告文件生成；Skill 的维护指导可能涉及实际变更，仍需依据用户的具体任务和授权执行。

报告包含本地路径、软件清单、设备标识、用户 SID、哈希化的主机标识和事件内容。哈希并不等于匿名化。不要把完整报告、转储、账户配置或凭据附到公开 issue 或 PR。

路径检查与枚举不锁定文件系统，不能抵御运行时恶意替换目录的攻击。请在可信本地环境使用，勿把本工具作为处理攻击者控制路径的删除授权器。

漏洞报告优先使用仓库 Security 页中的私密报告入口（若可用）。如果没有私密入口，只开一个不包含利用细节或敏感数据的 issue 请求联系。当前不承诺响应时间，也不应把尚未复测的修正当作系统安全保证。
