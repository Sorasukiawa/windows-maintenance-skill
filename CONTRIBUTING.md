# 贡献指南

请先用隔离夹具复现问题，说明 Windows 构建、PowerShell 版本、命令、预期和实际结果。提供脱敏的最小输入，不上传真实用户的软件清单、事件 XML、转储或完整配置备份。

修改脚本后运行 `tests/Invoke-CI.ps1`，同时检查 Windows PowerShell 5.1 与 PowerShell 7。新增系统采集只在需要的具体环境验证；默认 CI 不运行真实系统维护。

保留以下约定：查询失败不等于空结果，部分统计不等于完整空间，来源变化不产生卸载结论，目录分类不授予删除许可。新的厂商更新支持应说明精确适用范围、配置迁移、失败处理、回退和实际版本/功能验证。

脚本使用 UTF-8 BOM 以兼容 Windows PowerShell 5.1 的中文；Markdown 使用 UTF-8。不要引入硬编码用户名、设备实例、令牌或个人路径。

新增依赖或删除行为应有明确用途和对应验证。请区分已运行的测试与尚未验证的设备功能，不根据 CI 通过宣称所有电脑均安全或稳定。
